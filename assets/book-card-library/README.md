# 真实书封轮播库

这是 Skill 自有的片头书封素材库，当前收录 24 张。生产脚本按 `manifest.json` 顺序选取，自动排除当期目标书名。

轮播数量不固定。`plan_book_carousel.py` 根据配音中书名首字的实际起声帧，为每张分配 3–4 帧。真实配音时码完成后必须重算，目标书封在书名起声帧替换最后一张轮播卡。

封面来自旧版 Coze 原型素材包，保留作为本地视频生产素材。正式公开发布前，按实际使用范围复核封面授权。
